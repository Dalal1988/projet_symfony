{% extends "base.html.twig" %}

{% block body %}

    <h2>{{ licence.name }}</h2>
		<img src="{{ asset ('images/' ~ licence.media) }}" alt="{{ licence.name }}">

  {% endfor %}
</table>
		

{% endblock %}